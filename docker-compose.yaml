services:
  db:
    image: bitnami/postgresql:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: auth
    ports:
      - "5432:5432"
    volumes:
      - ./docker:/docker-entrypoint-initdb.d
  api:
    build: 
      context: .
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: "postgresql://postgres:postgres@db:5432/auth?schema=public"
      PORT: 3000

      JWT_SECRET: "457afa305430b3761121b945126e90d40ca0562d3737e661"
      JWT_TOKEN_AUDIENCE: "http://localhost:3000"
      JWT_TOKEN_ISSUER: "http://localhost:3000"
      JWT_TTL: 3600
      JWT_REFRESH_TTL: 84600

      MAIL_HOST: "smtp.gmail.com"
      MAIL_PORT: 587
      MAIL_USER: "mouravieira44@gmail.com"
      MAIL_PASSWORD: "thyc llbq hbpd mgcc"
      MAIL_FROM_NAME: "API - SERVICE"
      MAIL_FROM_EMAIL: "mouravieira44@gmail.com"

      STRIPE_SECRET_KEY: "adawsefa srgfargargar asr farefa sre"
      STRIPE_WEBHOOK_SECRET: "adaoeunpesufasuefapseubfaseubfba"

      GOOGLE_CLIENT_ID: "xxxx.apps.googleusercontent.com"
      GOOGLE_CLIENT_SECRET: "xxxx"
      GOOGLE_REDIRECT_URI: "http://localhost:3000/google/callback"

      REDIS_HOST: "redis"
      REDIS_PORT: 6379
    depends_on:
      - db
      - redis
    container_name: api
    ports:
      - "3000:3000"
  redis:
    image: redis:latest
    ports:
      - "6379:6379"